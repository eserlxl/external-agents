"""Minimal sample module for the live-smoke read-only sandbox."""


def add(a, b):
    """Return the sum of a and b."""
    return a + b


def is_even(n):
    """Return True if n is even."""
    return n % 2 == 0
